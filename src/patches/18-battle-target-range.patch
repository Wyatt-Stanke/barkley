diff -ruN a/objects/oBattleMenu/3_0.gml b/objects/oBattleMenu/3_0.gml
--- a/objects/oBattleMenu/3_0.gml
+++ b/objects/oBattleMenu/3_0.gml
@@ -175,7 +175,7 @@
   if (sKey(global.key_up,1)) { sP(); pos1=max(0,pos1-1); }
   if (sKey(global.key_down,1)) { sP(); pos1+=1; }
   sIntTarget(0,1);
-  if (target[pos1]="") pos1-=1;
+  while (pos1>0 && (pos1>=array_length(target) || target[pos1]="")) pos1-=1;
   hilight=targetid[pos1];
  } else if (pretarget='Enemy') {
   dname=1;
@@ -183,9 +183,10 @@
   if (sKey(global.key_up,1)) { sP(); pos1=max(0,pos1-2); }
   if (sKey(global.key_left,1)) { sP(); pos1=max(0,pos1-1); }
   if (sKey(global.key_right,1)) { sP(); pos1+=1; }
-  if (sKey(global.key_down,1)) { sP(); pos1+=1; if (target[pos1]="") pos1-=1; pos1+=1; }
+  if (sKey(global.key_down,1)) { sP(); pos1+=1; if (pos1>=array_length(target) || target[pos1]="") pos1-=1; pos1+=1; }
   sIntTarget(1);
-  if (target[pos1]="") pos1-=1;
+  //Right and down in one step went two past the last enemy: GM6 read target[] past its end as 0, LTS stops the game
+  while (pos1>0 && (pos1>=array_length(target) || target[pos1]="")) pos1-=1;
   hilight=targetid[pos1];
  } else if (pretarget="Self") {
   global.b_obj=global.turn;
