diff -ruN a/objects/oStartConfig/0_0.gml b/objects/oStartConfig/0_0.gml
--- a/objects/oStartConfig/0_0.gml
+++ b/objects/oStartConfig/0_0.gml
@@ -17,7 +17,8 @@
 instance_create(x,y+48,oStartConfig);
 }
 if (img=0) { global.sat[0]=min(global.sat[0],1); s1='Integer          Sharp fit'; s0='SCALING'; xs=x+18; spc=59; global.tt[img]=2; } //Modernized: Integer or Sharp fit (see oScreenFill)
-if (img=1) { s1='Windowed      Full Screen'; s0='SCREEN'; xs=x+9; spc=69; global.tt[img]=2; }
+if (img=1) { fsseen=fullscreen_get(); global.sat[1]=fsseen; //Modernized: shows the real state
+s1='Windowed      Full Screen'; s0='SCREEN'; xs=x+9; spc=69; global.tt[img]=2; }
 if (img=2) { s1='1      2      3      4      5'; s0='BRIGHTNESS'; xs=x+18; spc=24; global.tt[img]=5; }
 if (img=3) { s1='Slow        Normal      Fast'; s0='TEXT SPEED'; xs=x+10; spc=49; global.tt[img]=3; }
 if (img=4) { s1='Default     Test          Exit'; s0='SETTINGS'; xs=x+8; spc=52; global.tt[img]=3; }
diff -ruN a/objects/oStartConfig/3_1.gml b/objects/oStartConfig/3_1.gml
--- a/objects/oStartConfig/3_1.gml
+++ b/objects/oStartConfig/3_1.gml
@@ -1,3 +1,4 @@
+if (img=1) { fsnow=fullscreen_get(); if (fsnow!=fsseen) { global.sat[1]=fsnow; fsseen=fsnow; } } //Modernized: follows the page, so leaving fullscreen with Esc shows Windowed
 if (key_makestuff.doall!=-1) exit;
 if (img=0) {
 
@@ -32,11 +33,11 @@
 
 if (sKey(global.key_cancel,1)) { sP(-1); global.edit=-1; }
 if (sKey(global.key_left,1) || sKey(vk_left,1)) { sP();
-if (global.edit!=-1) global.sat[global.edit]=max(0,global.sat[global.edit]-1);
+if (global.edit!=-1) { global.sat[global.edit]=max(0,global.sat[global.edit]-1); if (global.edit=1) fullscreen_set(global.sat[1]); } //Modernized: SCREEN applies at once
 else { if (global.control<3) global.control=9; else if (global.control=3 || global.control=4) global.control=5; }
 }
 if (sKey(global.key_right,1) || sKey(vk_right,1)) { sP();
-if (global.edit!=-1) global.sat[global.edit]=min(global.tt[global.edit]-1,global.sat[global.edit]+1);
+if (global.edit!=-1) { global.sat[global.edit]=min(global.tt[global.edit]-1,global.sat[global.edit]+1); if (global.edit=1) fullscreen_set(global.sat[1]); } //Modernized: SCREEN applies at once
 else { if (global.control=9) global.control=0; else if (global.control=5) global.control=3; }
 }
 }
\ No newline at end of file
