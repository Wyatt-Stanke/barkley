{
  "name": "barkley-web",
  "private": true,
  "description": "The page around the HTML5 build: its UI (SolidJS), the runtime glue and the service worker",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "solid-js": "1.9.15"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vite": "8.3.1",
    "vite-plugin-solid": "2.11.14"
  }
}
